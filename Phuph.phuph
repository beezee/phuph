```phuph
repl{ "does this work?"; }
phuph```

#This is an example

Example is generated by running `phuph Phuph.phuph > Example.MD`

```phuph

function example($i) {
  return ['foo' => (1 * $i), 'bar' => (2 * $i)];
}

repl{ example(7); }
repl{ example(9); }
phuph```

```plaincode
```phuph
do some stuff
phuph```
plaincode```

And here's the end
